<prop category="" description="The number of missile and torpedo warhead types in the design" display_text="Number of missile and torpedo warhead types" name="num-ammo" rank="0" version="1.0"><tpcl_display>(lambda (design bits)
   (let ((n (apply + bits)))
      (cons n (string-append (number-&gt;string n) " explosive types"))))</tpcl_display><tpcl_requires>(lambda (design)
   (cons #t ""))</tpcl_requires></prop>