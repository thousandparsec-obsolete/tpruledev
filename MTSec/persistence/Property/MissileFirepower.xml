<prop category="" description="How much damage a missile can do" display_text="Missile Firepower" name="MissileFirepower" rank="1" version="1.0"><tpcl_display>(lambda (design bits)
   (let ((n (apply + bits)))
      (cons n (string-append (number-&gt;string n) " hit points")) ) )</tpcl_display><tpcl_requires>(lambda (design)
   (cons #t ""))</tpcl_requires></prop>