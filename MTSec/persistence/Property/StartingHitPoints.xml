<prop category="" description="How much damage the ship can take before being destroyed" display_text="Initial Hit Points" name="StartingHitPoints" rank="0" version="1.0"><tpcl_display>(lambda (design bits)
   (let ((n (apply + bits)))
      (cons n (string-append (number-&gt;string n) " hit points")) ) )</tpcl_display><tpcl_requires>(lambda (design)
   (cons #t ""))</tpcl_requires></prop>