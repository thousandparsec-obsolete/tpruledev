<prop category="" description="How much damage the weapons of the ship can do in a single round" display_text="Firepower" name="Firepower" rank="2" version="1.0"><tpcl_display>(lambda (design bits)
   (let ((n (apply + bits)))
   (cons n (string-append (number-&gt;string n) " hit points")) ) )</tpcl_display><tpcl_requires>(lambda (design)
   (cons #t ""))</tpcl_requires></prop>