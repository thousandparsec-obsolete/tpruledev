<property
   description="Can the ship colonise planets"
   display_text="Can Colonise Planets"
   name="Colonise"
   rank="0"
   version="1.1">
   <tpcl_display>(lambda (design bits)
   (let ((n (apply + bits)))
      (cons n (if (= n 1) "Yes" "No")) ) )   </tpcl_display>
   <tpcl_requires>(lambda (design)
   (cons #t ""))   </tpcl_requires>
   <!-- categories -->
   <category
      name="" />
</property>
